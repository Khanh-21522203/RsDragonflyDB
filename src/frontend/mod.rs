pub mod router;
pub mod acceptor;
pub mod handler;
pub mod multi_key;
pub mod error_handling;

pub fn add(left: u64, right: u64) -> u64 {
    left + right
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn it_works() {
        let result = add(2, 2);
        assert_eq!(result, 4);
    }
}
